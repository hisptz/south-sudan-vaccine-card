import { SelectionNamePipe } from './selection-name.pipe';

describe('SelectionNamePipe', () => {
  it('create an instance', () => {
    const pipe = new SelectionNamePipe();
    expect(pipe).toBeTruthy();
  });
});
