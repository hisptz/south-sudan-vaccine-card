import { TableListSearchPipe } from "./table-list-search.pipe";

describe("TableListSearchPipe", () => {
  it("create an instance", () => {
    const pipe = new TableListSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
