import { TableHeadersPipe } from './table-headers.pipe';

describe('TableHeadersPipe', () => {
  it('create an instance', () => {
    const pipe = new TableHeadersPipe();
    expect(pipe).toBeTruthy();
  });
});
