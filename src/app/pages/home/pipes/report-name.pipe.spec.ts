import { ReportNamePipe } from './report-name.pipe';

describe('ReportNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ReportNamePipe();
    expect(pipe).toBeTruthy();
  });
});
